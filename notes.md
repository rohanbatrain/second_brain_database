For "login" (i.e., issuing a new token), always require the user's credentials (username/email + password, and 2FA if enabled).
Do not allow login or sensitive actions based solely on token validation.

